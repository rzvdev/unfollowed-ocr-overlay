<UserControl x:Class="Unfollowed.App.Views.DiagnosticsTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid>
                <TextBlock Text="Diagnostics" FontSize="18" FontWeight="SemiBold" />
                <Button
                        Content="Clear logs"
                        Command="{Binding ClearLogsCommand}"
                        Padding="12,6"
                        Margin="106,0,0,0" />
            </Grid>
            <UniformGrid Grid.Row="1" Columns="2" Margin="0,16,0,16">
                <Border Style="{StaticResource SurfaceCard}" Padding="12" Margin="0,0,12,0">
                    <StackPanel>
                        <TextBlock Text="Average frame time" Style="{StaticResource SecondaryText}" />
                        <TextBlock Text="{Binding FrameTimeAverage, StringFormat={}{0:0.0} ms}"
                                   FontSize="16"
                                   FontWeight="SemiBold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource SurfaceCard}" Padding="12">
                    <StackPanel>
                        <TextBlock Text="Average OCR latency" Style="{StaticResource SecondaryText}" />
                        <TextBlock Text="{Binding OcrLatencyAverage, StringFormat={}{0:0.0} ms}"
                                   FontSize="16"
                                   FontWeight="SemiBold" />
                    </StackPanel>
                </Border>
            </UniformGrid>
            <ListBox Grid.Row="2"
                     ItemsSource="{Binding LogEntries}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}"
                                       Style="{StaticResource SecondaryText}" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Level}"
                                       Margin="8,0,0,0"
                                       Style="{StaticResource SecondaryText}" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding Category}"
                                       Margin="8,0,0,0"
                                       Style="{StaticResource SecondaryText}" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding Message}"
                                       Margin="8,0,0,0"
                                       TextWrapping="Wrap" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
